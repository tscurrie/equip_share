does this work
<%= link_to "Add Item", add_item_path, class: "btn btn-primary"%>
<% @items.each do |item|%>
	

	<div>
	  <h3><%= link_to item.name, item_path(item) %></h3>
	</div>
	<br>
<% end %>

<%= link_to "Edit", edit_item_path, class: "btn btn-primary"%>
<%= button_to "Delete", @item, method: :delete %>